schemas:
  SentAt:
    type: object
    $id: SentAt
    properties:
      sentAt:
        type: string
        format: date-time
        description: Datum en tijd dat bericht gestuurd is.
    required:
      - sentAt
